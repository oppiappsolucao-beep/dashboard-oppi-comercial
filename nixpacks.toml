[phases.setup]
nixPkgs = ["...", "libreoffice"]
aptPkgs = ["...", "libreoffice-writer-nogui", "fontconfig", "fonts-dejavu-core"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port 8501 --proxy-headers --timeout-keep-alive 5 --limit-concurrency 100"

[variables]
APP_BUILD = "20260724-proposals-qa-v32"
