[phases.setup]
nixPkgs = ["...", "libreoffice"]
aptPkgs = ["...", "libreoffice-writer-nogui", "fontconfig", "fonts-dejavu-core"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port 8501 --proxy-headers"

[variables]
APP_BUILD = "20260724-dept-proposals-v29"
