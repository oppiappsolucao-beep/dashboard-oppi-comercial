[phases.setup]
nixPkgs = ["...", "libreoffice"]
aptPkgs = ["...", "libreoffice-writer-nogui", "fontconfig", "fonts-dejavu-core"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port 8501 --workers 1 --proxy-headers --timeout-keep-alive 5 --limit-concurrency 80"

[variables]
APP_BUILD = "20260812-01-fix-att-storm-v163"
